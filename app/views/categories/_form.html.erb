<%= form_with model: @category, local: true, data: { turbo: false } do |f| %>
  <%= render 'shared/error', model: @category %>
    <%= f.label :title %><br>
    <%= f.text_field :title %><br>
  <%= f.submit %>
<% end %>